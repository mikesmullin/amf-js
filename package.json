{
  "name": "amf-js",
  "version": "1.0.2",
  "description": "Automatic Message Format (AMF0 + AMF3-compatible) HTML5 Javascript Library",
  "main": "src/amf.js",
  "scripts": {
    "test": "nodemon --watch ./ --delay 1 --exec 'mocha --reporter spec --bail --ui bdd --colors --timeout 1000 test/*.js'"
  },
  "author": "Mike Smullin <mike@smullindesign.com>",
  "license": "GPLv1",
  "devDependencies": {
    "chai": "^3.5.0",
    "mocha": "^2.5.3",
    "nodemon": "^1.9.2"
  }
}
